on:
 push :
   branches:
    - master


jobs:
  build:
   runs-on: ubuntu-latest
  steps:
    -name: 'printig folder contents before '
     run: ls
    - uses: actions/checkout@v3
    - name: "Login to docker"
      run: docker login --username yasmiin --password dckr_pat_3LHkgeI1AlErf-Q7yy1zgD1HqIk


